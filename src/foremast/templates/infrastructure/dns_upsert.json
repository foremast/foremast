{
    "Comment": "automatic change",
    "Changes": [
        {
            "Action": "UPSERT",
            "ResourceRecordSet": {
                "Name": "{{ dns_elb }}",
                "Type": "CNAME",
                "TTL": {{ dns_ttl }},
                "ResourceRecords": [
                    {
                        "Value": "{{ dns_elb_aws }}"
                    }
                ]
            }
        }
    ]
}
