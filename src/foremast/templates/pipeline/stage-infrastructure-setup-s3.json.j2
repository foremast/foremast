  {
      "requisiteStageRefIds": [""],
      "refId": "master",
      "type": "jenkins",
      "comments": "This stage will run the infrastructure setup scripts to setup s3 buckets, s3 bucket policies, s3 bucket ACLs in <b>{{ data.app.environment }}</b>. This will look at the applications runway/ configuration files to determine setup",
      "name": "Infrastructure Setup S3 [{{ data.app.environment }}]",
      "failPipeline": true,
      "master": "JenkinsCI",
      "job": "s3-prepare",
      "parameters": {
        "PROJECT": "{{ data.app.group_name }}",
        "GIT_REPO": "{{ data.app.repo_name }}",
        "ENV": "{{ data.app.environment }}",
        "REGION": "{{ data.app.region }}",
        "OWNER_EMAIL": "{{ data.app.owner_email}}",
        "APP_NAME": "{{ data.app.appname }}"
        }
    }
