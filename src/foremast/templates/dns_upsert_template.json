{
    "Comment": "automatic change",
    "Changes": [
        {
            "Action": "UPSERT",
            "ResourceRecordSet": {
                "Name": "{{ dns_elb }}",
                "Type": "CNAME",
                "TTL": 300,
                "ResourceRecords": [
                    {
                        "Value": "{{ dns_elb_aws }}"
                    }
                ]
            }
        }
    ]
}
