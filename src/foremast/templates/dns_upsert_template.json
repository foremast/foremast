{
    "Comment": "automatic change",
    "Changes": [
        {
            "Action": "UPSERT",
            "ResourceRecordSet": {
                "Name": "{{ data.dns_elb }}",
                "Type": "CNAME",
                "TTL": 60,
                "ResourceRecords": [
                    {
                        "Value": "{{ data.dns_elb_aws }}"
                    }
                ]
            }
        }
    ]
}
